"""Общие таблицы и мелкие помощники: раскладки бланков, месяцы, пути, пароли.

Файл пуст по содержанию, но нужен: без него каталог остаётся неявным пакетом, и
статический анализ видит `utils/constants.py` дважды — как `constants` и как
`utils.constants`, — после чего отказывается проверять что-либо (INFRA-7).
"""
