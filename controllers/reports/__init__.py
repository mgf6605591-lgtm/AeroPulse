"""Построители отчётов: по модулю на форму (ARCH-15).

Все четыре жили в одном `data_controller.py` на 1288 строк — своды 12-ГА, сводка
15-ГА по аэропортам, бланк одного аэропорта и подробная таблица делили файл и
пространство имён. Свалки там не было: функции короткие и объяснённые, — но чтобы
поправить сводку 15-ГА, приходилось открывать файл, где 900 строк про 12-ГА.

Разрез прошёл по форме, и границу подсказали сами имена функций: префиксы
`_ga12_` и `_ga15_` уже выполняли роль пакетов, только без их гарантий.

    common.py        периоды колонок и приведение сумм — общее всем формам
    ga12_pivot.py    четыре свода 12-ГА
    ga15_metrics.py  код графы 15-ГА и отбор по нему — общее двум сводам ниже
    ga15_summary.py  сводка по всем аэропортам
    ga15_airport.py  бланк одного аэропорта

`data_controller.py` остался диспетчером: выбирает построителя по режиму и числу
выбранных предприятий и собирает подробную таблицу.
"""
